# OpenRouter boundary instrumentation proxy — nginx logs per-call upstream timing
FROM nginx:alpine
COPY examples/cookbook/jarvis/bench/nginx-openrouter.conf /etc/nginx/nginx.conf
RUN mkdir -p /logs
EXPOSE 8080
