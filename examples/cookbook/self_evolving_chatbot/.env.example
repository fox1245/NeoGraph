# Copy to .env or use --env-file with an existing file. Never commit the real key.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=z-ai/glm-5.3-flash
# Optional dedicated PostgreSQL database; omit to use SQLite.
# NEOGRAPH_CHAT_POSTGRES_URL=postgresql://USER:PASSWORD@127.0.0.1:PORT/DATABASE
