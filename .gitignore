build/
build-*/
docs/api/
.env
.claude/
.playwright-mcp
benchmarks/node_modules/

# Python venvs (e.g. projects/re_agent/.venv for the scorer).
.venv/
__pycache__/
*.pyc

# Session handoff note — local planning doc, not tracked.
NEXT_SESSION.md

# Local VSCode multi-folder workspace files.
*.code-workspace

# Stray CMake artifacts if cmake is ever run from the repo root by accident
/CMakeCache.txt
/CMakeFiles/
/Makefile
/cmake_install.cmake
/compile_commands.json

# RE analysis artifacts — may be derivative work, not for public push.
# Keep them on disk for personal use; private repo backup if needed.
projects/re_agent/results/
projects/re_agent/targets/j2kengine_ground_truth.json
Testing/
